---
phase: 03-operational-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/instrumentation.ts"]
autonomous: true
requirements: [GATEWAY-01]

must_haves:
  truths:
    - "Application connects to gateway automatically on startup without any HTTP call"
    - "Connection uses GATEWAY_IP and GATEWAY_PORT from environment variables"
    - "If gateway is unreachable, app still starts (connect fails gracefully)"
  artifacts:
    - path: "src/instrumentation.ts"
      provides: "Gateway auto-connect on server startup"
      contains: "gatewayService.connect"
  key_links:
    - from: "src/instrumentation.ts"
      to: "src/lib/gateway/GatewayService.ts"
      via: "import + gatewayService.connect()"
      pattern: "gatewayService\\.connect\\("
---

<objective>
Add gateway auto-connect to server startup so the application connects to the inSona gateway automatically when the Next.js server starts, using IP and port from environment variables.

Purpose: GATEWAY-01 — app must connect gateway on startup without manual API call or HTTP trigger.
Output: Modified instrumentation.ts with gateway auto-connect.
</objective>

<execution_context>
@${HOME}/.claude/get-shit-done/workflows/execute-plan.md
@${HOME}/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-production-runtime/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/gateway/GatewayService.ts:
```typescript
// Singleton export
export const gatewayService = new GatewayService();

// Connection method (line 47)
connect(ip: string, port: number = 8091): Promise<void>

// Status properties
get isConnected(): boolean    // true when _status === "connected"
get status(): string          // "connected" | "disconnected" | "connecting" | "reconnecting"
```

From src/instrumentation.ts (current state):
```typescript
export async function register() {
  if (process.env.NEXT_RUNTIME === 'nodejs') {
    console.log('[Instrumentation] 服务器启动中...');
    const { startScheduler } = await import('./lib/scheduler/BackgroundScheduler');
    startScheduler();
    console.log('[Instrumentation] 后台服务已启动');
  }
}
```

From .env.example:
```
GATEWAY_IP="192.168.1.100"
GATEWAY_PORT=8091
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add gateway auto-connect to instrumentation.ts</name>
  <files>src/instrumentation.ts</files>
  <behavior>
    - Test 1: When GATEWAY_IP is set in env, register() calls gatewayService.connect(ip, port)
    - Test 2: When GATEWAY_IP is not set, register() logs a warning but does NOT throw
    - Test 3: When gatewayService.connect() throws, register() catches and logs error (app still starts)
    - Edge case: GATEWAY_PORT defaults to 8091 if not set in env
  </behavior>
  <read_first>
    - src/instrumentation.ts (current startup hook)
    - src/lib/gateway/GatewayService.ts (connect method signature, line 47)
    - .env.example (GATEWAY_IP, GATEWAY_PORT env var names)
  </read_first>
  <acceptance_criteria>
    - src/instrumentation.ts contains `import { gatewayService }` or dynamic import of GatewayService singleton
    - src/instrumentation.ts contains `gatewayService.connect(` call after `startScheduler()`
    - Connection IP is read from `process.env.GATEWAY_IP` (not hardcoded)
    - Connection port is read from `process.env.GATEWAY_PORT` with default 8091
    - The connect() call is wrapped in try/catch — failure does NOT crash the server
    - A console.log message confirms connection attempt: `[Instrumentation] 正在连接网关 {ip}:{port}`
    - On success: console.log `[Instrumentation] 网关连接成功: {ip}:{port}`
    - On missing env: console.warn `[Instrumentation] GATEWAY_IP 未配置，跳过网关连接`
    - On error: console.error `[Instrumentation] 网关连接失败: {error message}`
  </acceptance_criteria>
  <action>
    Modify src/instrumentation.ts to add gateway auto-connect after the scheduler start.

    The modified file should:
    1. Import or dynamically import the gatewayService singleton from @/lib/gateway/GatewayService
    2. After startScheduler(), check if process.env.GATEWAY_IP exists
    3. If GATEWAY_IP is set, call gatewayService.connect(GATEWAY_IP, parseInt(process.env.GATEWAY_PORT || "8091"))
    4. Wrap the connect call in try/catch so startup never fails due to gateway being unreachable
    5. Log appropriate messages in Chinese matching the acceptance criteria patterns

    Use dynamic import (like the existing scheduler import) to keep it server-only:
    ```
    const { gatewayService } = await import('@/lib/gateway/GatewayService');
    ```

    The connect() call should be fire-and-wait (await the Promise), not fire-and-forget, so we can log success/failure.
    But wrap it in try/catch so a failed connection does NOT prevent the Next.js server from starting.

    What to avoid:
    - Do NOT call /api/gateway/autoconnect via HTTP (that route reads from DB which may not have a gateway row yet)
    - Do NOT use fetch() to hit the API route — call gatewayService.connect() directly
    - Do NOT make the server fail to start if gateway connection fails
  </action>
  <verify>
    <automated>grep -c "gatewayService.connect" src/instrumentation.ts && grep -c "GATEWAY_IP" src/instrumentation.ts</automated>
  </verify>
  <done>
    - instrumentation.ts imports gatewayService and calls connect() using GATEWAY_IP/GATEWAY_PORT env vars
    - Connection attempt is logged with [Instrumentation] prefix
    - Errors are caught and logged, server startup is not blocked by gateway failure
    - No HTTP/fetch call to /api/gateway/autoconnect route
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→gateway | TCP connection to internal IP (GATEWAY_IP from .env) — internal network only |
| env→container | GATEWAY_IP/GATEWAY_PORT passed as env vars to container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | gatewayService.connect() | accept | Gateway is on isolated internal network; no auth on TCP 8091 by design |
| T-03-02 | Information Disclosure | env vars (GATEWAY_IP) | mitigate | GATEWAY_IP passed via .env file (not committed), docker-compose env_file directive |
| T-03-03 | Denial of Service | instrumentation.ts connect failure | mitigate | try/catch ensures server starts even if gateway unreachable; scheduler retry (SchedulerCore line 217-231) provides recovery |
</threat_model>

<verification>
## Gateway Auto-Connect Verification

1. Start the app: `docker compose up -d --build`
2. Check logs: `docker compose logs insona-admin 2>&1 | grep -c "正在连接网关"` — should return 1
3. If gateway is reachable: should see `[Instrumentation] 网关连接成功`
4. If gateway is unreachable: should see `[Instrumentation] 网关连接失败` but app still responds on :3000
5. Verify app responds: `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000` → 200
</verification>

<success_criteria>
- [ ] GATEWAY-01 satisfied: application connects gateway on startup using .env config
- [ ] No HTTP call to /api/gateway/autoconnect — direct gatewayService.connect() call
- [ ] Server starts even if gateway connection fails (try/catch confirmed in source)
- [ ] Source code contains GATEWAY_IP env var reference with 8091 default port
- [ ] Log messages match acceptance criteria patterns
</success_criteria>

<output>
After completion, create `.planning/phases/03-operational-verification/03-01-SUMMARY.md`
</output>
