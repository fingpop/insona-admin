---
phase: 03-operational-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/gateway/EnergyLogger.ts", "src/lib/gateway/GatewayService.ts", "docker-compose.yml", "deploy/logrotate.conf"]
autonomous: true
requirements: [LOG-02, GATEWAY-02]

must_haves:
  truths:
    - "Energy events are appended to a dedicated log file in JSON-lines format"
    - "Log file path is configurable via env var, defaults to /app/data/logs/energy.log"
    - "Gateway reconnect works in production Docker (exponential backoff verified)"
  artifacts:
    - path: "src/lib/gateway/EnergyLogger.ts"
      provides: "File-based energy event logger with JSON-lines format"
      exports: ["logEnergyEvent"]
      min_lines: 20
    - path: "src/lib/gateway/GatewayService.ts"
      provides: "Modified _handleEnergyEvent to call EnergyLogger"
      contains: "EnergyLogger"
    - path: "docker-compose.yml"
      provides: "GATEWAY_IP/GATEWAY_PORT env vars + log volume mount"
      contains: "GATEWAY_IP"
    - path: "deploy/logrotate.conf"
      provides: "Logrotate configuration for energy.log"
      contains: "rotate"
  key_links:
    - from: "src/lib/gateway/GatewayService.ts"
      to: "src/lib/gateway/EnergyLogger.ts"
      via: "import + logEnergyEvent() call in _handleEnergyEvent"
      pattern: "EnergyLogger|logEnergyEvent"
    - from: "docker-compose.yml"
      to: "src/lib/gateway/EnergyLogger.ts"
      via: "LOG_PATH env var matching default path"
      pattern: "LOG_PATH|energy\\.log"
---

<objective>
Create file-based energy event logging with logrotate configuration, and ensure gateway auto-reconnect works in production Docker.

Purpose: LOG-02 — energy events must be written to independent file with rotation support. GATEWAY-02 — verify reconnect logic works in production.
Output: EnergyLogger.ts utility, GatewayService integration, logrotate config, Docker env updates.
</objective>

<execution_context>
@${HOME}/.claude/get-shit-done/workflows/execute-plan.md
@${HOME}/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-production-runtime/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/gateway/GatewayService.ts _handleEnergyEvent (line 549):
```typescript
private async _handleEnergyEvent(msg: Record<string, unknown>) {
  const { did, power, percent, period, meshid, energy } = msg;
  // ... existing DB logic (lines 550-761) ...
}
```

The msg object structure from inSona gateway:
```json
{
  "method": "s.event",
  "evt": "energy",
  "did": "ECC57FB5134F00",
  "power": 100,
  "percent": 50,
  "period": 5,
  "meshid": "MESH001",
  "energy": [1, 80, 2, 90]
}
```

From GatewayService.ts hardcoded device-specific log (line 558-586, to be REPLACED):
```typescript
// Current: hardcoded single-device log to energy_events.log
if (did === "ECC57FB5134F00") {
  const logEntry = `[${timeStr}] ${JSON.stringify(msg)}${analysis}\n`;
  const logPath = path.join(process.cwd(), "energy_events.log");
  fs.appendFileSync(logPath, logEntry, "utf8");
}
```

The NEW EnergyLogger module should export:
```typescript
export function logEnergyEvent(msg: Record<string, unknown>): void;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create EnergyLogger utility and integrate into GatewayService</name>
  <files>src/lib/gateway/EnergyLogger.ts, src/lib/gateway/GatewayService.ts</files>
  <behavior>
    - Test 1: logEnergyEvent writes one JSON line per call to the configured log file
    - Test 2: Log directory is created automatically if it doesn't exist
    - Test 3: Each line is valid JSON (parseable with JSON.parse)
    - Test 4: Each line contains: timestamp (ISO 8601), deviceId, event data
    - Edge case: If log file cannot be written (permission error), log to console.error instead of throwing
  </behavior>
  <read_first>
    - src/lib/gateway/GatewayService.ts (lines 549-586: _handleEnergyEvent with hardcoded log)
    - src/lib/gateway/GatewayService.ts (line 578: current log format to understand what data is logged)
    - Dockerfile (to understand container file structure, /app/data directory)
  </read_first>
  <acceptance_criteria>
    - src/lib/gateway/EnergyLogger.ts exists with exported function `logEnergyEvent(msg: Record<string, unknown>): void`
    - EnergyLogger reads log path from process.env.LOG_PATH, defaulting to "/app/data/logs/energy.log"
    - EnergyLogger creates parent directory (e.g., /app/data/logs/) using fs.mkdirSync with recursive:true if it doesn't exist
    - Each log line is JSON format: `{"timestamp":"2026-04-15T10:30:00.000Z","deviceId":"ECC57FB5134F00","power":100,"percent":50,"period":5,"energy":[1,80,2,90]}`
    - EnergyLogger uses fs.appendFileSync for synchronous write (non-blocking is not needed for log events)
    - EnergyLogger wraps fs.appendFileSync in try/catch — on failure, calls console.error("[EnergyLogger] Write failed:", err)
    - GatewayService._handleEnergyEvent: the hardcoded single-device log block (lines 558-586, the `if (did === "ECC57FB5134F00")` block) is REPLACED with `EnergyLogger.logEnergyEvent(msg)` call
    - GatewayService imports EnergyLogger at top of file
    - The logEnergyEvent call is for ALL devices (no device ID filter), replacing the device-specific hardcoded log
    - GatewayService line 279-281: the `_handleEnergyEvent` call for "energy" events is preserved unchanged
  </acceptance_criteria>
  <action>
    Step A — Create src/lib/gateway/EnergyLogger.ts:

    Create a new utility module that writes energy events to a file in JSON-lines format.
    - Export a single function: `logEnergyEvent(msg: Record<string, unknown>): void`
    - Read log path from `process.env.LOG_PATH`, default to `"/app/data/logs/energy.log"`
    - Use an IIFE or module-level init to ensure the parent directory exists:
      ```typescript
      const LOG_PATH = process.env.LOG_PATH || "/app/data/logs/energy.log";
      const LOG_DIR = path.dirname(LOG_PATH);
      // Ensure directory exists (runs once on import)
      try { fs.mkdirSync(LOG_DIR, { recursive: true }); } catch { /* ignore */ }
      ```
    - The function writes one JSON line: `{timestamp, deviceId, ...rest of msg fields}`
    - Use `JSON.stringify()` on a new object that includes a new Date().toISOString() timestamp
    - Append with `\n` suffix for JSON-lines format
    - Wrap in try/catch — on error, console.error

    Step B — Modify src/lib/gateway/GatewayService.ts:

    1. Add import at top: `import { logEnergyEvent } from "./EnergyLogger";`
    2. Find the hardcoded single-device log block in _handleEnergyEvent (the `if (did === "ECC57FB5134F00")` block at lines 558-586)
    3. REPLACE that entire block (the if statement + its contents) with a single call:
       ```
       logEnergyEvent(msg);
       ```
    4. This call should happen BEFORE the device existence check (line 589), so it logs even for unknown devices
    5. Do NOT modify any other code in _handleEnergyEvent

    What to avoid:
    - Do NOT add async file writes (sync is fine for log events, keeps code simple)
    - Do NOT add log rotation logic in code (logrotate handles that at OS level)
    - Do NOT keep the hardcoded device ID check — log ALL energy events
    - Do NOT modify the DB persistence logic (EnergyData, EnergyHourly, EnergyRecord)
  </action>
  <verify>
    <automated>grep -c "logEnergyEvent" src/lib/gateway/GatewayService.ts && grep -c "ECC57FB5134F00" src/lib/gateway/GatewayService.ts | grep -q "^0$"</automated>
  </verify>
  <done>
    - EnergyLogger.ts created with logEnergyEvent export, writes JSON-lines to configurable path
    - GatewayService uses EnergyLogger for ALL energy events (no device filter)
    - The hardcoded ECC57FB5134F00-specific log block is completely removed from GatewayService
    - Log directory auto-created on module import
    - Write failures are caught and logged to console.error
  </done>
</task>

<task type="auto">
  <name>Task 2: Add GATEWAY env vars to Docker and create logrotate config</name>
  <files>docker-compose.yml, deploy/logrotate.conf</files>
  <read_first>
    - docker-compose.yml (current env_file and environment configuration)
    - .env.example (GATEWAY_IP, GATEWAY_PORT variable names)
    - Dockerfile (container directory structure)
  </read_first>
  <acceptance_criteria>
    - docker-compose.yml environment section contains `GATEWAY_IP` (value from .env)
    - docker-compose.yml environment section contains `GATEWAY_PORT` (value from .env)
    - docker-compose.yml environment section contains `LOG_PATH=/app/data/logs/energy.log`
    - docker-compose.yml volumes section includes `- ./data:/app/data` (already exists, verify preserved)
    - docker-compose.yml volumes section includes a log volume: the ./data volume already covers /app/data which includes the logs subdirectory — no additional volume needed
    - deploy/logrotate.conf contains:
      - Target path: `/opt/insona-admin/data/logs/energy.log` (host path, not container path)
      - `daily` rotation
      - `rotate 30` (keep 30 days)
      - `compress`
      - `delaycompress`
      - `missingok`
      - `notifempty`
      - `copytruncate` (app can keep writing to the same file)
      - `dateext` (date-stamped rotated files)
      - `dateformat -%Y%m%d`
  </acceptance_criteria>
  <action>
    Step A — Modify docker-compose.yml:

    Add GATEWAY_IP, GATEWAY_PORT, and LOG_PATH to the environment section.
    The current environment section has only DATABASE_URL. Add the gateway env vars.

    The environment section should look like:
    ```yaml
    environment:
      - DATABASE_URL=file:/app/data/dev.db
      - GATEWAY_IP=${GATEWAY_IP}
      - GATEWAY_PORT=${GATEWAY_PORT}
      - LOG_PATH=/app/data/logs/energy.log
    ```

    The GATEWAY_IP and GATEWAY_PORT use ${} syntax to read from the .env file (docker-compose variable substitution).
    LOG_PATH is hardcoded because it's a container-internal path.

    Also verify that GATEWAY_IP and GATEWAY_PORT are NOT already in environment (they should only be in env_file).
    The current .env_file directive already loads .env, but explicit environment vars ensure they're available even if .env is missing one.

    Step B — Create deploy/logrotate.conf:

    Create a logrotate configuration file for the energy log file on the host server.
    The log file on the host is at /opt/insona-admin/data/logs/energy.log (matching the systemd WorkingDirectory).

    The file should be a standard logrotate config block:
    ```
    /opt/insona-admin/data/logs/energy.log {
        daily
        rotate 30
        compress
        delaycompress
        missingok
        notifempty
        copytruncate
        dateext
        dateformat -%Y%m%d
    }
    ```

    `copytruncate` is used instead of creating a new file + signaling the process because:
    - The Node.js app holds an fd to the file and doesn't support log rotation signals
    - copytruncate copies then truncates the original, so the app keeps writing to the same fd

    Document in a comment at the top how to install:
    ```
    # Install: sudo cp deploy/logrotate.conf /etc/logrotate.d/insona-admin
    # Test: sudo logrotate -d /etc/logrotate.d/insona-admin
    ```
  </action>
  <verify>
    <automated>grep -q "GATEWAY_IP" docker-compose.yml && grep -q "LOG_PATH" docker-compose.yml && grep -q "copytruncate" deploy/logrotate.conf</automated>
  </verify>
  <done>
    - docker-compose.yml passes GATEWAY_IP, GATEWAY_PORT, LOG_PATH as environment variables to the container
    - deploy/logrotate.conf created with daily rotation, 30-day retention, copytruncate, compress
    - Log file path in logrotate matches deployment WorkingDirectory (/opt/insona-admin/data/logs/energy.log)
    - Install instructions included as comment in logrotate.conf
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app→log file | Energy events written to local file — not transmitted over network |
| container→host volume | /app/data mounted as ./data volume — log file accessible on host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | energy.log | accept | Log contains device power/energy data only, no PII or credentials |
| T-03-05 | Denial of Service | log file growth | mitigate | logrotate with daily rotation + 30-day retention + compress limits disk usage |
| T-03-06 | Tampering | energy.log | accept | Internal network deployment, single admin user, file permissions sufficient |
| T-03-07 | Repudiation | reconnect (GATEWAY-02) | mitigate | GatewayService._scheduleReconnect() logs each attempt via debug() → console.log → docker logs (PROCESS-04 already covers) |
</threat_model>

<verification>
## Energy Logging Verification

1. Start app: `docker compose up -d --build`
2. Trigger energy event (or wait for gateway energy report)
3. Check log file exists: `docker compose exec insona-admin ls -la /app/data/logs/energy.log`
4. Check log format: `docker compose exec insona-admin tail -5 /app/data/logs/energy.log`
5. Verify each line is valid JSON: `docker compose exec insona-admin tail -5 /app/data/logs/energy.log | while read line; do echo "$line" | python3 -m json.tool > /dev/null && echo "OK" || echo "INVALID: $line"; done`

## Gateway Reconnect Verification

1. Verify reconnect config in GatewayService.ts:
   - `grep "maxReconnectAttempts" src/lib/gateway/GatewayService.ts` → should show `private maxReconnectAttempts: number = 10;`
   - `grep "Math.pow(2," src/lib/gateway/GatewayService.ts` → should show exponential backoff formula
   - `grep "60000" src/lib/gateway/GatewayService.ts` → should show max 60s delay
2. In production Docker: disconnect gateway physically, check docker logs for reconnect attempts with increasing delays
</verification>

<success_criteria>
- [ ] LOG-02 satisfied: energy events written to file in JSON-lines format, all devices (not just ECC57FB5134F00)
- [ ] EnergyLogger.ts created with configurable log path via LOG_PATH env var
- [ ] Hardcoded single-device log block removed from GatewayService._handleEnergyEvent
- [ ] docker-compose.yml includes GATEWAY_IP, GATEWAY_PORT, LOG_PATH in environment
- [ ] GATEWAY-02 satisfied: GatewayService reconnect logic confirmed (exponential backoff, max 10 attempts, max 60s delay)
- [ ] deploy/logrotate.conf created with daily rotation, 30-day retention, copytruncate, compress
- [ ] No additional volumes needed for log (./data:/app/data already covers logs subdirectory)
</success_criteria>

<output>
After completion, create `.planning/phases/03-operational-verification/03-02-SUMMARY.md`
</output>
