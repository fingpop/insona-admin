---
phase: 08-panel-scene-linkage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/app/api/panel-bindings/route.ts
  - src/app/api/panel-bindings/[id]/route.ts
  - src/lib/gateway/GatewayService.ts
  - src/app/(dashboard)/control/page.tsx
autonomous: true
requirements:
  - PSL-01
  - PSL-02
  - PSL-03
  - PSL-04
  - PSL-05
user_setup: []

must_haves:
  truths:
    - "用户可在面板联动 Tab 中输入面板 DID 并选择场景进行按键绑定"
    - "绑定关系展示为列表，支持删除和更换场景"
    - "网关推送 switch.key 事件时，系统自动查找绑定并激活对应场景"
    - "按键按下（value[1]=1）触发场景执行，抬起不触发"
    - "每次按键触发场景执行后，DashboardEvent 表中新增一条日志记录"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "PanelSceneBinding model"
      contains: "model PanelSceneBinding"
    - path: "src/app/api/panel-bindings/route.ts"
      provides: "GET/POST bindings API"
      exports: ["GET", "POST"]
    - path: "src/app/api/panel-bindings/[id]/route.ts"
      provides: "PUT/DELETE binding API"
      exports: ["PUT", "DELETE"]
    - path: "src/lib/gateway/GatewayService.ts"
      provides: "switch.key event handler"
      contains: "_handleSwitchKeyEvent"
  key_links:
    - from: "src/lib/gateway/GatewayService.ts"
      to: "prisma.PanelSceneBinding"
      via: "findUnique query on panelDid+buttonIndex"
      pattern: "panelSceneBinding\\.findUnique"
    - from: "src/lib/gateway/GatewayService.ts"
      to: "prisma.DashboardEvent"
      via: "create log entry after scene activation"
      pattern: "dashboardEvent\\.create"
    - from: "src/app/(dashboard)/control/page.tsx"
      to: "/api/panel-bindings"
      via: "fetch calls for CRUD operations"
      pattern: "fetch.*panel-bindings"

---

<objective>
实现面板按键场景联动：数据库表 + API + 网关事件处理 + 控制面板 UI

Purpose: 用户可为面板设备（func=9）的按键绑定场景，网关推送 switch.key 事件时自动执行对应场景
Output: PanelSceneBinding 模型、CRUD API、GatewayService 事件处理、面板联动 Tab UI
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-panel-scene-linkage/08-CONTEXT.md
@prisma/schema.prisma
@src/lib/gateway/GatewayService.ts
@src/app/(dashboard)/control/page.tsx
@src/app/api/scenes/route.ts
@src/app/api/scenes/activate/route.ts
@src/lib/gateway/MultiGatewayService.ts
@src/lib/types.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From prisma/schema.prisma — Scene model:
```prisma
model Scene {
  id        String   @id @default(cuid())
  name      String
  icon      String   @default("fa-star")
  color     String   @default("#3b9eff")
  isDefault Boolean  @default(false)
  isCustom  Boolean  @default(false)
  showInQuick Boolean @default(false)
  sceneId   Int?     // 网关场景ID（可选）
  meshId    String?  // 关联的 mesh
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  actions   SceneAction[]
}
```

From src/lib/gateway/GatewayService.ts — key methods:
```typescript
// Message dispatch (line 281-317):
private _handleMessage(msg: Record<string, unknown>) {
  // method === "s.event" branches:
  //   evt === "energy" → _handleEnergyEvent
  //   evt === "onoff" || evt === "status" → _handleDeviceEvent
  //   → NEW: evt === "switch.key" → _handleSwitchKeyEvent
}

// Scene activation (line 517-528):
async activateScene(sceneId: number, meshid: string): Promise<unknown>

// DashboardEvent creation pattern (from _handleDeviceEvent, line 582-595):
await prisma.dashboardEvent.create({
  data: {
    type: eventType,
    deviceId: did as string,
    message: eventMessage,
    status: "unread",
    metadata: JSON.stringify(msg),
  },
});
```

From src/app/api/panel-bindings/route.ts (to be created):
```typescript
// GET: returns { bindings: PanelSceneBinding[], scenes: Scene[] }
// POST body: { panelDid: string, buttonIndex: number, sceneId: string }
```

From src/app/api/panel-bindings/[id]/route.ts (to be created):
```typescript
// PUT body: { sceneId: string } — replace bound scene
// DELETE: remove binding
```

switch.key event format (from CONTEXT.md):
```json
{
  "version": 1,
  "uuid": 10352,
  "method": "s.event",
  "evt": "switch.key",
  "did": "ECC57F10C831FF",
  "func": 9,
  "value": [0, 1]
}
```
- value[0] = button index (0=key1, 1=key2, ...)
- value[1] = button state (1=pressed, 0=released)
- Trigger only on pressed (value[1] === 1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 数据库模型 + Prisma 迁移 + CRUD API</name>
  <files>prisma/schema.prisma, src/app/api/panel-bindings/route.ts, src/app/api/panel-bindings/[id]/route.ts</files>
  <read_first>
    - prisma/schema.prisma
    - src/app/api/scenes/route.ts
    - src/app/api/panel-bindings/route.ts (if exists)
  </read_first>
  <action>
## 1.1 添加 PanelSceneBinding 模型到 prisma/schema.prisma

在 DashboardEvent 模型之前（或文件末尾，DashboardEvent 之后）添加：

```prisma
model PanelSceneBinding {
  id          String   @id @default(cuid())
  panelDid    String   // 面板设备 DID（不需要在 Device 表中存在）
  buttonIndex Int      // 按键索引：0=按键1, 1=按键2, ...
  sceneId     String   // 绑定的场景 ID（Scene 表的 id 字段）
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  scene       Scene    @relation(fields: [sceneId], references: [id])

  @@unique([panelDid, buttonIndex])
  @@index([panelDid])
  @@index([sceneId])
}
```

注意：
- `panelDid` 不需要是 Device 表中存在的 DID（用户可手动输入任意 DID）
- `sceneId` 是外键关联 Scene 表的 `id`（cuid），不是 `sceneId` 字段（网关场景 ID）
- 组合唯一约束 `@@unique([panelDid, buttonIndex])` 防止同一按键重复绑定

## 1.2 运行迁移

```bash
npx prisma migrate dev --name add_panel_scene_binding
npx prisma generate
```

## 1.3 创建 GET/POST API — src/app/api/panel-bindings/route.ts

参考 src/app/api/scenes/route.ts 的模式：

```typescript
import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";

export const runtime = "nodejs";

// GET — 获取所有绑定关系 + 场景列表（用于下拉选择）
export async function GET() {
  try {
    const [bindings, scenes] = await Promise.all([
      prisma.panelSceneBinding.findMany({
        include: { scene: true },
        orderBy: { createdAt: "desc" },
      }),
      prisma.scene.findMany({
        where: { sceneId: { not: null } }, // 只有配置了网关场景ID的场景才能被触发
        orderBy: { name: "asc" },
      }),
    ]);
    return Response.json({ bindings, scenes });
  } catch (err) {
    console.error("Failed to fetch panel bindings:", err);
    return NextResponse.json({ error: "获取绑定列表失败" }, { status: 500 });
  }
}

// POST — 创建新的按键绑定
export async function POST(request: Request) {
  try {
    const body = await request.json();
    const { panelDid, buttonIndex, sceneId } = body;

    if (!panelDid || buttonIndex === undefined || buttonIndex === null || !sceneId) {
      return NextResponse.json({ error: "缺少必要参数" }, { status: 400 });
    }

    // 验证 sceneId 存在
    const scene = await prisma.scene.findUnique({ where: { id: sceneId } });
    if (!scene) {
      return NextResponse.json({ error: "场景不存在" }, { status: 404 });
    }

    const binding = await prisma.panelSceneBinding.create({
      data: {
        panelDid: panelDid.toUpperCase(), // 规范化 DID 为大写
        buttonIndex: Number(buttonIndex),
        sceneId,
      },
      include: { scene: true },
    });

    return Response.json({ binding });
  } catch (err) {
    if (err && typeof err === "object" && "code" in err && (err as any).code === "P2002") {
      return NextResponse.json({ error: "该按键已有绑定" }, { status: 409 });
    }
    console.error("Failed to create panel binding:", err);
    return NextResponse.json({ error: "创建绑定失败" }, { status: 500 });
  }
}
```

## 1.4 创建 PUT/DELETE API — src/app/api/panel-bindings/[id]/route.ts

```typescript
import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";

export const runtime = "nodejs";

// PUT — 更换绑定场景
export async function PUT(
  request: Request,
  { params }: { params: { id: string } }
) {
  try {
    const body = await request.json();
    const { sceneId } = body;

    if (!sceneId) {
      return NextResponse.json({ error: "缺少场景ID" }, { status: 400 });
    }

    const binding = await prisma.panelSceneBinding.update({
      where: { id: params.id },
      data: { sceneId },
      include: { scene: true },
    });

    return Response.json({ binding });
  } catch (err) {
    if (err && typeof err === "object" && "code" in err && (err as any).code === "P2025") {
      return NextResponse.json({ error: "绑定不存在" }, { status: 404 });
    }
    console.error("Failed to update panel binding:", err);
    return NextResponse.json({ error: "更新绑定失败" }, { status: 500 });
  }
}

// DELETE — 删除绑定
export async function DELETE(
  _request: Request,
  { params }: { params: { id: string } }
) {
  try {
    await prisma.panelSceneBinding.delete({
      where: { id: params.id },
    });
    return Response.json({ result: "ok" });
  } catch (err) {
    if (err && typeof err === "object" && "code" in err && (err as any).code === "P2025") {
      return NextResponse.json({ error: "绑定不存在" }, { status: 404 });
    }
    console.error("Failed to delete panel binding:", err);
    return NextResponse.json({ error: "删除绑定失败" }, { status: 500 });
  }
}
```
  </action>
  <verify>
    <automated>npx prisma db execute --stdin --schema prisma/schema.prisma &lt; /dev/null 2>&1; grep -q "model PanelSceneBinding" prisma/schema.prisma && echo "MODEL_OK" || echo "MODEL_FAIL"</automated>
  </verify>
  <done>PanelSceneBinding 模型已添加到 schema.prisma，migration 已执行，4 个 CRUD API 端点已创建并可正常工作</done>
</task>

<task type="auto">
  <name>Task 2: GatewayService 新增 switch.key 事件处理</name>
  <files>src/lib/gateway/GatewayService.ts</files>
  <read_first>
    - src/lib/gateway/GatewayService.ts
  </read_first>
  <action>
在 GatewayService 中新增 switch.key 事件处理方法，并在 _handleMessage 中注册分支。

## 2.1 在 _handleMessage 中添加 switch.key 分支

在 `_handleMessage` 方法中（约第 298-313 行），在 `energy` 事件处理之后、`onoff/status` 处理之前或之后，添加：

```typescript
else if (msg.evt === "switch.key") {
  this._handleSwitchKeyEvent(msg).catch((err) => {
    debug("[SWITCH.KEY] Failed to process:", err);
  });
}
```

完整修改后的事件分支区域（约第 302-314 行）应类似：

```typescript
// 处理能耗上报事件
if (msg.evt === "energy") {
  this._handleEnergyEvent(msg).catch((err) => {
    debug("[ENERGY] Failed to save:", err);
  });
}
// 处理面板按键事件
else if (msg.evt === "switch.key") {
  this._handleSwitchKeyEvent(msg).catch((err) => {
    debug("[SWITCH.KEY] Failed to process:", err);
  });
}
// 处理设备状态变化事件（创建 DashboardEvent）
else if (msg.evt === "onoff" || msg.evt === "status") {
  this._handleDeviceEvent(msg).catch((err) => {
    debug("[DEVICE EVENT] Failed to save:", err);
  });
}
```

## 2.2 新增 _handleSwitchKeyEvent 私有方法

在 `_handleDeviceEvent` 方法之前（约第 548 行之前）添加：

```typescript
// ─── Switch key event handling ─────────────────────────────────────────────

private async _handleSwitchKeyEvent(msg: Record<string, unknown>) {
  const { did, func, value } = msg;

  if (!did) {
    debug("[SWITCH.KEY] Missing device ID");
    return;
  }

  // 解析按键值
  if (!Array.isArray(value) || value.length < 2) {
    debug("[SWITCH.KEY] Invalid value format:", value);
    return;
  }

  const buttonIndex = value[0] as number;
  const buttonState = value[1] as number;

  // 只在按下时触发（value[1] === 1），抬起不触发
  if (buttonState !== 1) {
    debug(`[SWITCH.KEY] Ignoring release event for button ${buttonIndex}`);
    return;
  }

  // 查找绑定关系
  const binding = await prisma.panelSceneBinding.findUnique({
    where: {
      panelDid_buttonIndex: {
        panelDid: did as string,
        buttonIndex,
      },
    },
    include: { scene: true },
  });

  if (!binding) {
    debug(`[SWITCH.KEY] No binding found for panel ${did} button ${buttonIndex}`);
    return;
  }

  // 获取场景的 meshId（从 scene.meshId 或通过 scene actions 获取）
  const meshId = binding.scene.meshId;
  if (!meshId) {
    debug(`[SWITCH.KEY] Scene ${binding.scene.name} has no meshId`);
    return;
  }

  // 获取场景的网关场景 ID（scene.sceneId，Int 类型）
  if (binding.scene.sceneId === null || binding.scene.sceneId === undefined) {
    debug(`[SWITCH.KEY] Scene ${binding.scene.name} has no gateway sceneId`);
    return;
  }

  try {
    // 激活场景
    await this.activateScene(binding.scene.sceneId, meshId);

    // 记录触发日志到 DashboardEvent
    await prisma.dashboardEvent.create({
      data: {
        type: "panel_button_trigger",
        message: `面板 ${(did as string).slice(-6)} 按键${buttonIndex + 1} 触发场景「${binding.scene.name}」`,
        status: "unread",
        metadata: JSON.stringify({
          panelDid: did,
          buttonIndex,
          buttonState,
          func,
          sceneName: binding.scene.name,
          sceneId: binding.scene.sceneId,
          meshId,
        }),
      },
    });

    debug(`[SWITCH.KEY] Activated scene "${binding.scene.name}" from panel ${did} button ${buttonIndex}`);
  } catch (err) {
    debug(`[SWITCH.KEY] Failed to activate scene:`, err);
  }
}
```

关键点：
- 使用 `did`（面板 DID）和 `buttonIndex` 查找绑定
- 只在 `buttonState === 1`（按下）时触发
- 场景激活需要 `scene.sceneId`（Int，网关场景 ID）和 `scene.meshId`（String）
- 两个都缺一不可，否则记录 debug 日志并跳过
- 触发后记录 DashboardEvent，type 为 "panel_button_trigger"
</action>
  <verify>
    <automated>grep -q "_handleSwitchKeyEvent" src/lib/gateway/GatewayService.ts && grep -q "switch\.key" src/lib/gateway/GatewayService.ts && grep -q "panel_button_trigger" src/lib/gateway/GatewayService.ts && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>GatewayService._handleMessage 已注册 switch.key 事件分支，_handleSwitchKeyEvent 方法正确查找绑定、激活场景、记录日志</done>
</task>

<task type="auto">
  <name>Task 3: 控制面板新增"面板联动"Tab UI</name>
  <files>src/app/(dashboard)/control/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/control/page.tsx
  </read_first>
  <action>
在 control/page.tsx 中创建"面板联动"子组件并集成到主页面。

## 3.1 创建 PanelSceneLinkage 子组件

在 control/page.tsx 文件中（找一个合适的组件定义位置，例如在能量分析组件之前或场景管理组件之后），添加一个新的子组件。参考现有设备管理组件的模式（DeviceManagement 组件约在 1240 行开始）：

```tsx
// ==================== 面板场景联动 ====================
function PanelSceneLinkage() {
  const [panelDid, setPanelDid] = useState("");
  const [buttonIndex, setButtonIndex] = useState(0);
  const [selectedScene, setSelectedScene] = useState("");
  const [bindings, setBindings] = useState<any[]>([]);
  const [scenes, setScenes] = useState<any[]>([]);
  const [loading, setLoading] = useState(false);
  const [saving, setSaving] = useState(false);
  const [editingId, setEditingId] = useState<string | null>(null);
  const [editSceneId, setEditSceneId] = useState("");

  // 加载绑定列表和场景列表
  const loadBindings = async () => {
    try {
      const res = await fetch("/api/panel-bindings");
      const data = await res.json();
      if (data.bindings) setBindings(data.bindings);
      if (data.scenes) setScenes(data.scenes);
    } catch (err) {
      console.error("Failed to load panel bindings:", err);
    }
  };

  useEffect(() => {
    loadBindings();
  }, []);

  // 创建绑定
  const handleCreate = async () => {
    if (!panelDid.trim()) {
      alert("请输入面板 DID");
      return;
    }
    if (!selectedScene) {
      alert("请选择场景");
      return;
    }
    setSaving(true);
    try {
      const res = await fetch("/api/panel-bindings", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          panelDid: panelDid.trim(),
          buttonIndex,
          sceneId: selectedScene,
        }),
      });
      const data = await res.json();
      if (!res.ok) {
        alert(data.error || "创建失败");
        return;
      }
      setPanelDid("");
      setButtonIndex(0);
      setSelectedScene("");
      await loadBindings();
    } catch (err) {
      alert("创建失败");
    } finally {
      setSaving(false);
    }
  };

  // 删除绑定
  const handleDelete = async (id: string) => {
    if (!confirm("确认删除该绑定？")) return;
    try {
      const res = await fetch(`/api/panel-bindings/${id}`, { method: "DELETE" });
      if (!res.ok) {
        const data = await res.json();
        alert(data.error || "删除失败");
        return;
      }
      await loadBindings();
    } catch (err) {
      alert("删除失败");
    }
  };

  // 开始编辑
  const startEdit = (binding: any) => {
    setEditingId(binding.id);
    setEditSceneId(binding.sceneId);
  };

  // 保存编辑
  const handleSaveEdit = async () => {
    if (!editingId || !editSceneId) return;
    try {
      const res = await fetch(`/api/panel-bindings/${editingId}`, {
        method: "PUT",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ sceneId: editSceneId }),
      });
      if (!res.ok) {
        const data = await res.json();
        alert(data.error || "更新失败");
        return;
      }
      setEditingId(null);
      setEditSceneId("");
      await loadBindings();
    } catch (err) {
      alert("更新失败");
    }
  };

  // 取消编辑
  const cancelEdit = () => {
    setEditingId(null);
    setEditSceneId("");
  };

  // 按键索引选项（0-5，6个按键）
  const buttonOptions = [0, 1, 2, 3, 4, 5];

  return (
    <div className="space-y-6">
      {/* 创建绑定表单 */}
      <div className="bg-[#1a1f2e] rounded-xl border border-white/5 p-6">
        <h3 className="text-lg font-semibold text-white mb-4">
          <i className="fa fa-link mr-2 text-blue-400"></i>
          新建按键绑定
        </h3>
        <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
          {/* 面板 DID 输入 */}
          <div>
            <label className="block text-sm text-gray-400 mb-1">面板 DID</label>
            <input
              type="text"
              value={panelDid}
              onChange={(e) => setPanelDid(e.target.value.toUpperCase())}
              placeholder="ECC57F10C831FF"
              className="w-full px-3 py-2 bg-[#0f1520] border border-white/10 rounded-lg text-white text-sm uppercase font-mono focus:border-blue-400 focus:outline-none"
            />
          </div>
          {/* 按键索引选择 */}
          <div>
            <label className="block text-sm text-gray-400 mb-1">按键</label>
            <select
              value={buttonIndex}
              onChange={(e) => setButtonIndex(Number(e.target.value))}
              className="w-full px-3 py-2 bg-[#0f1520] border border-white/10 rounded-lg text-white text-sm focus:border-blue-400 focus:outline-none"
            >
              {buttonOptions.map((idx) => (
                <option key={idx} value={idx}>按键 {idx + 1}</option>
              ))}
            </select>
          </div>
          {/* 场景选择 */}
          <div>
            <label className="block text-sm text-gray-400 mb-1">绑定场景</label>
            <select
              value={selectedScene}
              onChange={(e) => setSelectedScene(e.target.value)}
              className="w-full px-3 py-2 bg-[#0f1520] border border-white/10 rounded-lg text-white text-sm focus:border-blue-400 focus:outline-none"
            >
              <option value="">选择场景</option>
              {scenes.map((scene) => (
                <option key={scene.id} value={scene.id}>
                  {scene.name}{scene.sceneId ? "" : " (无网关ID)"}
                </option>
              ))}
            </select>
          </div>
          {/* 创建按钮 */}
          <div className="flex items-end">
            <button
              onClick={handleCreate}
              disabled={saving}
              className="w-full px-4 py-2 bg-blue-500 hover:bg-blue-600 disabled:opacity-50 text-white rounded-lg text-sm font-medium transition-colors"
            >
              {saving ? "创建中..." : "创建绑定"}
            </button>
          </div>
        </div>
      </div>

      {/* 绑定列表 */}
      <div className="bg-[#1a1f2e] rounded-xl border border-white/5 overflow-hidden">
        <div className="px-6 py-4 border-b border-white/5">
          <h3 className="text-lg font-semibold text-white">
            <i className="fa fa-list mr-2 text-blue-400"></i>
            已有绑定
          </h3>
        </div>
        {bindings.length === 0 ? (
          <div className="text-center py-12 text-gray-500">
            <i className="fa fa-link text-3xl mb-3"></i>
            <p>暂无绑定关系，请创建新的按键绑定</p>
          </div>
        ) : (
          <table className="w-full">
            <thead>
              <tr className="border-b border-white/5">
                <th className="text-left px-6 py-3 text-xs font-medium text-gray-400 uppercase">面板 DID</th>
                <th className="text-left px-6 py-3 text-xs font-medium text-gray-400 uppercase">按键</th>
                <th className="text-left px-6 py-3 text-xs font-medium text-gray-400 uppercase">绑定场景</th>
                <th className="text-left px-6 py-3 text-xs font-medium text-gray-400 uppercase">创建时间</th>
                <th className="text-right px-6 py-3 text-xs font-medium text-gray-400 uppercase">操作</th>
              </tr>
            </thead>
            <tbody>
              {bindings.map((binding) => (
                <tr key={binding.id} className="border-b border-white/5 hover:bg-white/5 transition-colors">
                  <td className="px-6 py-3 font-mono text-sm text-gray-300">
                    {binding.panelDid}
                  </td>
                  <td className="px-6 py-3">
                    <span className="inline-flex items-center px-2 py-1 rounded-md bg-blue-500/20 text-blue-400 text-xs font-medium">
                      按键 {binding.buttonIndex + 1}
                    </span>
                  </td>
                  <td className="px-6 py-3 text-sm text-gray-300">
                    {editingId === binding.id ? (
                      <select
                        value={editSceneId}
                        onChange={(e) => setEditSceneId(e.target.value)}
                        className="px-2 py-1 bg-[#0f1520] border border-white/10 rounded text-sm text-white focus:border-blue-400 focus:outline-none"
                      >
                        {scenes.map((scene) => (
                          <option key={scene.id} value={scene.id}>{scene.name}</option>
                        ))}
                      </select>
                    ) : (
                      <span className="flex items-center gap-2">
                        <i className={`fa ${binding.scene?.icon || "fa-star"} mr-1`} style={{ color: binding.scene?.color || "#3b9eff" }}></i>
                        {binding.scene?.name || "未知场景"}
                      </span>
                    )}
                  </td>
                  <td className="px-6 py-3 text-sm text-gray-500">
                    {new Date(binding.createdAt).toLocaleString("zh-CN")}
                  </td>
                  <td className="px-6 py-3 text-right">
                    {editingId === binding.id ? (
                      <div className="flex items-center justify-end gap-2">
                        <button
                          onClick={handleSaveEdit}
                          className="px-2 py-1 text-xs bg-green-500/20 text-green-400 rounded hover:bg-green-500/30 transition-colors"
                        >
                          <i className="fa fa-check mr-1"></i>保存
                        </button>
                        <button
                          onClick={cancelEdit}
                          className="px-2 py-1 text-xs bg-gray-500/20 text-gray-400 rounded hover:bg-gray-500/30 transition-colors"
                        >
                          <i className="fa fa-times mr-1"></i>取消
                        </button>
                      </div>
                    ) : (
                      <div className="flex items-center justify-end gap-2">
                        <button
                          onClick={() => startEdit(binding)}
                          className="px-2 py-1 text-xs bg-yellow-500/20 text-yellow-400 rounded hover:bg-yellow-500/30 transition-colors"
                        >
                          <i className="fa fa-edit mr-1"></i>更换
                        </button>
                        <button
                          onClick={() => handleDelete(binding.id)}
                          className="px-2 py-1 text-xs bg-red-500/20 text-red-400 rounded hover:bg-red-500/30 transition-colors"
                        >
                          <i className="fa fa-trash mr-1"></i>删除
                        </button>
                      </div>
                    )}
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        )}
      </div>
    </div>
  );
}
```

## 3.2 在主页面中集成面板联动

在 ControlPanel 主组件（`export default function ControlPanel()`）中找到页面渲染的 switch 语句（搜索 `switch (currentPage)` 或类似模式），在现有 case 中添加：

```tsx
case "panel-linkage":
  return <PanelSceneLinkage />;
```

在 pageTitles 对象中添加：
```tsx
"panel-linkage": { title: "面板联动", subtitle: "面板按键场景绑定" },
```

注意：如果当前 ControlPanel 是单一组件（所有 Tab 都在一个组件内），需要在对应的 Tab 渲染区域添加 `panel-linkage` 的处理。查看 ControlPanel 组件中 `currentPage` 的路由逻辑，找到渲染分支。
  </action>
  <verify>
    <automated>grep -q "PanelSceneLinkage" src/app/(dashboard)/control/page.tsx && grep -q "panel-linkage" src/app/(dashboard)/control/page.tsx && grep -q "面板联动" src/app/(dashboard)/control/page.tsx && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>控制面板中新增"面板联动"功能区域，包含创建表单和绑定列表，支持输入 DID、选择按键索引和场景、创建/删除/更换绑定</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway → Server (switch.key event) | Untrusted input from gateway, must validate DID/buttonIndex/sceneId |
| Client → API (POST/PUT /api/panel-bindings) | User-supplied data, must validate panelDid format, buttonIndex range, sceneId existence |
| Server → Gateway (activateScene) | Trusted internal call, but sceneId/meshId must be validated before sending |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | GatewayService._handleSwitchKeyEvent | mitigate | Validate `value` is array with length >= 2, `buttonState === 1` before triggering; reject non-numeric buttonIndex |
| T-08-02 | Tampering | POST /api/panel-bindings | mitigate | Validate `buttonIndex` in range 0-5, `panelDid` non-empty string, `sceneId` exists in DB; reject duplicate bindings via unique constraint (P2002) |
| T-08-03 | Spoofing | switch.key event processing | mitigate | Only trigger scene activation when exact binding match found in PanelSceneBinding table; no fallback or default behavior |
| T-08-04 | Repudiation | Scene activation logging | mitigate | Every activation logged to DashboardEvent with full metadata (panelDid, buttonIndex, sceneId, meshId, timestamp) |
| T-08-05 | Information Disclosure | API responses | accept | Only binding and scene data returned; no sensitive data exposed; internal network deployment |
</threat_model>

<verification>
1. `npx prisma migrate dev` 成功，PanelSceneBinding 表创建
2. `GET /api/panel-bindings` 返回空绑定列表和场景列表
3. `POST /api/panel-bindings` 创建绑定成功，重复创建返回 409
4. `PUT /api/panel-bindings/:id` 更换场景成功
5. `DELETE /api/panel-bindings/:id` 删除绑定成功
6. 模拟 switch.key 事件（value=[0,1]）→ 查找绑定 → activateScene 被调用 → DashboardEvent 创建
7. 模拟 switch.key 事件（value=[0,0]）→ 不触发场景（抬起事件）
8. 控制面板"面板联动"区域可见，表单可输入 DID、选择按键和场景
9. 创建后绑定列表展示新条目，支持删除和更换场景
</verification>

<success_criteria>
- [ ] PanelSceneBinding 模型存在于 schema.prisma，包含 panelDid, buttonIndex, sceneId 字段和组合唯一约束
- [ ] Prisma migration 已执行，`npx prisma db push` 无错误
- [ ] GET /api/panel-bindings 返回 { bindings, scenes }，scenes 仅包含 sceneId != null 的场景
- [ ] POST /api/panel-bindings 创建绑定，panelDid 规范化为大写
- [ ] GatewayService._handleSwitchKeyEvent 方法存在，正确处理 value=[0,1] 触发、value=[0,0] 忽略
- [ ] 场景激活后 DashboardEvent 表新增 type="panel_button_trigger" 的记录
- [ ] 控制面板可见"面板联动"功能区域，包含创建表单和绑定列表表格
- [ ] 绑定列表支持删除（红色按钮）和更换场景（黄色编辑→保存/取消）
</success_criteria>

<output>
After completion, create `.planning/phases/08-panel-scene-linkage/08-01-SUMMARY.md`
</output>
